execution_engine: asyncio
logger:
  transports: [console]
  level: info

mcp:
  servers:
    fetch:
      command: "uvx"
      args: ["mcp-server-fetch"]

    filesystem:
      command: "npx"
      args: ["-y", "@modelcontextprotocol/server-filesystem", "."]
    
    pluto:
      command: "node"
      args: ["/Users/sohamvijay/Desktop/Pluto/src/main.ts"]
      description: "Rootstock MCP server for sending funds and blockchain operations"

google:
  default_model: gemini-2.0-flash
  temperature: 0.7
  vertexai: false
